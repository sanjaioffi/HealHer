import '../interface/hive_manager.dart';

class HiveManagerImpl implements HiveManager {
  @override
  Future<void> readFromHive(String key) {
    throw UnimplementedError();
  }

  @override
  Future<void> writeToHive(String key, value) {
    throw UnimplementedError();
  }
}
